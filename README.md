To install the dependencies for both frontend and backend simultaneously, run the following command in the root directory:

**npm run install-all**

To start both the frontend and backend concurrently, use the following command:

**npm start**